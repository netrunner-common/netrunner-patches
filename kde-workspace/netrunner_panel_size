Description: Set default panel size to 33
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 kde-workspace (4:4.11.2-1netrunner2) saucy; urgency=low
 .
   * Bumped version number
Author: Leszek Lesner <leszek@zevenos.com>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: http://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: <YYYY-MM-DD>

--- kde-workspace-4.11.2.orig/plasma/desktop/containments/panel/panel.cpp
+++ kde-workspace-4.11.2/plasma/desktop/containments/panel/panel.cpp
@@ -90,7 +90,7 @@ protected:
 Panel::Panel(QObject *parent, const QVariantList &args)
     : Containment(parent, args),
       m_configureAction(0),
-      m_currentSize(QSize(Kephal::ScreenUtils::screenSize(screen()).width(), 35)),
+      m_currentSize(QSize(Kephal::ScreenUtils::screenSize(screen()).width(), 33)),
       m_maskDirty(true),
       m_canResize(true),
       m_spacerIndex(-1),
@@ -145,9 +145,9 @@ void Panel::init()
 
     m_currentSize = cg.readEntry("minimumSize", m_currentSize);
     if (formFactor() == Plasma::Vertical) {
-        m_currentSize.expandedTo(QSize(0, 35));
+        m_currentSize.expandedTo(QSize(0, 33));
     } else {
-        m_currentSize.expandedTo(QSize(35, 0));
+        m_currentSize.expandedTo(QSize(33, 0));
     }
 
     setMinimumSize(cg.readEntry("minimumSize", m_currentSize));
